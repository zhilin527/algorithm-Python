# -*- coding: UTF-8 -*-#*## Tornado vs Gevent vs Asyncio# Tornado并发网络库和同时也是一个Web位框架# Gevent绿色线程greenlet实现并发，猴子补丁修改内置socket# Asyncio python3内置的并发网络库，基于原生协程# Tornado适用于微服务，实现Restful接口# 1。底层基于Linux IO多路复用# 2。可以通过协程或者回调实现异步编程# 3。不过生态不完整，相应的异步框架比如ORM不完善# Gevent 高性能的并发网络库# 1。基于轻量级绿色线程greenlet实现并发# 2。需要注意的是monkey patch，gevent修改了内置的socket改为非阻塞# 3。配合gunicorn和gevent部署作为wsgi server#################### *#