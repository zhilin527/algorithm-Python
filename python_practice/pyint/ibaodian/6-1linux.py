#linux命令及基础#man 查询linux命令 #man的替代品 tldr，使用pip3 install tldr安装#pip3 install --upgrade pip#pip3 --help##tar#linux系统文件存储方式：# 文件是存储在硬盘上的，硬盘的最小存储单位叫做扇区sector，每个扇区存储512字节。# 操作系统读取硬盘的时候，不会一个个扇区地读取，这样效率太低，而是一次性连续读取多个扇区，即一次性读取一个块block。# 这种由多个扇区组成的块，是文件存取的最小单位。块的大小，最常见的是4KB，即连续八个sector组成一个block。## 文件数据存储在块中，那么还必须找到一个地方存储文件的元信息，比如文件的创建者、文件的创建日期、文件的大小等等。# 这种存储文件元信息的区域就叫做inode，中文译名为索引节点，也叫i节点。# 因此，一个文件必须占用一个inode，但至少占用一个block。## 元信息 → inode# 数据 → block#常见文件目录操作#1。chown,chmod,chgrp#2。ls/rm/cd/cp/mv/touch/rename/ln 软链接和硬链接#3。locate/find/grep 定位查找和搜索#文件查看#1。编辑器 vi/nano#2。cat/head/tail 查看文件#3。more/less 交互式查看文件#进程操作命令#1。ps查看进程#2。kill杀死进程 kill执行的原理是什么？#3。top/htop监控进程#内存操作命令#1。free查看可用内存#2。了解每一列的含义#3。排查内存泄漏问题